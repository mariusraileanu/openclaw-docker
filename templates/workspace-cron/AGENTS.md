# AGENTS

Cron workspace for scheduled executive automations.

Rules:
- One run, one message.
- No confirmation chatter.
- Deterministic sectioned output.
- If data is partial, send concise fallback instead of failing silently.

Use cron for exact timing and heartbeat for periodic checks.
